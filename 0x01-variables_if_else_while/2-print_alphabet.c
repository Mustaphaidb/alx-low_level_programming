#include <stdio.h>

/**
 * main - this is an Entry point
 *
 * @void - tasks no parametres
 *
 * Description - 'TASK2 print alphabet'
 * Return: 0 (always)
 *
*/

int main(void)
{

char A;
for (A = 'a'; A <= 'z'; A++)
putchar(A);
putchar('\n');

return (0);
}
